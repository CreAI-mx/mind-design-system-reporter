import { ArtifactForm } from '@/components/artifacts/artifact-form'

export const metadata = { title: 'Nuevo Artifact — LIPU Mind DS Reporter' }

export default function NewArtifactPage() {
  return <ArtifactForm mode="create" />
}
