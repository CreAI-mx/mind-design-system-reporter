import { ArtifactList } from '@/components/artifacts/artifact-list'

export const metadata = { title: 'Artifacts — LIPU Mind DS Reporter' }

export default function ArtifactsPage() {
  return <ArtifactList />
}
